<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorBlack</item> <!-- 기본 텍스트 색상 -->
        <item name="android:itemBackground">@color/colorWrite</item> <!-- 아이템 배경 색상 -->
    </style>

    <style name="MaterialButton_1" parent="Theme.AppCompat">
        <item name="android:textColor">@color/colorWrite</item>
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="colorControlHighlight">@color/colorWrite</item>
    </style>

    <style name="MaterialButton_2" parent="Theme.AppCompat">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <item name="colorButtonNormal">@color/colorWrite</item>
        <item name="colorControlHighlight">@color/colorPrimaryDark</item>
    </style>

    <style name="TransparentDisplay" parent="@style/Theme.AppCompat">
        <!-- 투명 디스플레이 스타일 -->
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <!-- 투명정도를 나타내는 색-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:backgroundDimEnabled">true</item>
        <!--
        android:backgroundDimEnabled
        true - 반투명 검정 /  false - 완전 투명
        -->
    </style>

</resources>
